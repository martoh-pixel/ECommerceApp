<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet permission for loading images -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Optional: For better image caching -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ECommerceApp"
        android:usesCleartextTraffic="true"> <!-- For development with HTTP URLs -->

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CartActivity"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".CheckoutActivity"
            android:parentActivityName=".CartActivity" />

        <activity
            android:name=".OrderConfirmationActivity"
            android:parentActivityName=".MainActivity" />

    </application>

</manifest>